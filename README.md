# داشبورد مرغداری

یک اپلیکیشن تحت وب برای مدیریت اطلاعات مرغداری‌ها با قابلیت ثبت گزارش روزانه، محاسبه دور فن، مدیریت سالن‌ها و پشتیبان‌گیری از اطلاعات.

## ویژگی‌های جدید
- **ذخیره خودکار اطلاعات در localStorage** (با رفرش صفحه از بین نمی‌رود).
- **خروجی و ورودی اطلاعات به صورت فایل JSON** برای پشتیبان‌گیری و بازیابی.
- مدیریت کاربران با سیستم ثبت‌نام و ورود ساده.

## نحوه استفاده
1. فایل `index.html` را در مرورگر باز کنید.
2. ثبت‌نام کرده یا وارد شوید.
3. از منوهای بالای صفحه برای مدیریت سالن‌ها، ثبت گزارش، محاسبه دور فن و ... استفاده کنید.
4. برای دریافت فایل پشتیبان، روی **📥 خروجی اطلاعات** کلیک کنید.
5. برای بازیابی اطلاعات از فایل، روی **📤 ورودی اطلاعات** کلیک کرده و فایل JSON را انتخاب کنید.

## فایل‌ها
- `index.html` : ساختار اصلی
- `style.css` : استایل‌ها
- `script.js` : منطق برنامه

## نکته
تمامی اطلاعات در **LocalStorage** مرورگر ذخیره می‌شود. با پاک کردن حافظه مرورگر، اطلاعات از بین خواهد رفت. برای حفظ اطلاعات، حتماً از قابلیت خروجی استفاده کنید.
